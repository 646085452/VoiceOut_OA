# Project Setup Instructions

1. In the folder "VoiceOut_OA/TestLogin", open `TestLogin.xcodeproj` on an OS system with Xcode installed to start browsing the project.
2. Click on top bar: `Product` -> `Build` -> `Run` to start the simulator.